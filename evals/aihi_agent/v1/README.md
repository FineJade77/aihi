# `aihi_agent` conformance corpus v1

This version contains the initial valid and intentionally invalid redacted
TraceBundle cases in `manifest.jsonl`. They are evaluated with
`HarnessConformanceRunner` and are safe to run offline. The corpus will grow by
adding cases without changing the meaning of these frozen entries.
